import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Elevation Coffee Heights — Concept Preview" };

export default function Page() {
  return (
    <ComingSoon
      business="Elevation Coffee Heights"
      hookHeadline="Your page title tag is literally empty."
      hookBody="The HTML title on elevationhaus.square.site is <title></title> — meaning your browser tab shows nothing and Google has no title to display in search results. Five-second fix, free. The bigger conversation: a real domain that tells your day-into-night story (coffee bar → La Sétima natural wine bar) instead of a Square subdomain."
    />
  );
}
