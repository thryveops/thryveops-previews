import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Coasis Restaurant — Concept Preview" };

export default function Page() {
  return (
    <ComingSoon
      business="Coasis Restaurant, Bar & Suites"
      hookHeadline="Your social media buttons go to dead-end Wix placeholders."
      hookBody="The Instagram and Facebook icons in your website footer don't point to @coasistampa — they point to generic Wix profile pages. Every guest clicking 'follow us' is hitting a dead end. Five-minute fix, silently bleeding followers on every page view."
    />
  );
}
