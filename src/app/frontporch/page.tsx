import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Front Porch & Bart's Tavern — Concept Preview" };

export default function Page() {
  return (
    <ComingSoon
      business="Front Porch & Bart's Tavern"
      hookHeadline="Your hero image — the porch itself — is broken."
      hookBody="For a 23-year-old Victorian house whose entire brand is 'the front porch,' the most damaging single element across our Tampa audit is the placeholder GIF where the porch photo should be. One photo upload, one upload — instant trust restored."
    />
  );
}
